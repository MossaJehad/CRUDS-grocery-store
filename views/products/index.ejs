<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category %> Products</title>
</head>
<body>
    <div class="section">
        <div class="columns is-flex is-justify-content-space-evenly">
            <h1 class="title is-1"><%= category %> Products</h1>
            <a href="/products/new"><button class="button is-info is-rounded is-medium ">Add New Product</button></a>
        </div>
        <div class="columns is-centered">
            <ul class="column is-6">
                <% for(let product of products){ %>
                    <li class="m-2 p-2"> 
                        <a href="/products/<%=product._id%>">
                            <div class="card">
                                <div class="card-content">
                                    <p class="title is-5"><%= product.name %></p>
                                </div>
                            </div>
                        </a> 
                    </li>
                <% } %>
            </ul>
        </div>
        <div class="columns is-flex is-justify-content-space-evenly">
            <% if(category !== 'All') {%>
                <a href="/products"><button class="button is-outlined is-info is-rounded is-medium ">All Products</button></a>
            <% } %>
        </div>
    </div>
</body>
</html>